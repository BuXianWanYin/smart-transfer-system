# Node 环境
NODE_ENV=development

# 应用标题
VITE_APP_TITLE=基于Java+Vue的TCP拥塞控制优化大文件传输工具

# 前端开发服务端口
VITE_APP_PORT=3000

# API 请求前缀（前端请求会发到 /api/xxx，由 Vite 代理转发）
VITE_API_BASE_URL=/api

# 接口超时时间（毫秒），30 秒
VITE_API_TIMEOUT=30000

# 后端接口地址（必须用 127.0.0.1，不能 localhost，否则 Clumsy 延迟会触发 Node Happy Eyeballs 250ms 超时）
VITE_SERVER_URL=http://127.0.0.1:8081

# 分片大小、最大文件大小：从系统配置接口 /file/upload/config 获取，与后端 application.yml 一致
